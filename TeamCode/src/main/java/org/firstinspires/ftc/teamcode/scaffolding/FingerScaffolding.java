package org.firstinspires.ftc.teamcode.scaffolding;

import org.firstinspires.ftc.teamcode.subsytem.FingerSubsystem;
import org.firstinspires.ftc.teamcode.subsytem.WristSubsystem;

import java.util.function.BooleanSupplier;

public class FingerScaffolding {
    /*    final FingerSubsystem finger;
    BooleanSupplier aButton;
    BooleanSupplier bButton;
    BooleanSupplier xButton;
    final double intakePosition = 0.3;
    final double barClearPosition = 0;
    final double scoringPosition = 0.5;

    public FingerScaffolding (FingerSubsystem finger, BooleanSupplier aButton, BooleanSupplier bButton, BooleanSupplier xButton){
        this.finger = finger;
        this.aButton = aButton;
        this.bButton = bButton;
        this.xButton = xButton;
        addRequirements(finger);
    }

    @Override
    public void execute() {
        if(aButton.getAsBoolean() == true) finger.moveFinger(intakePosition);
        if(bButton.getAsBoolean() == true) finger.moveFinger(barClearPosition);
        if(xButton.getAsBoolean() == true) finger.moveFinger(scoringPosition);

    }*/


}
